import React from 'react';
import { TextField, TextFieldProps } from '@mui/material';


const TextFieldUi: React.FC<TextFieldProps> = (props) => {
  return <TextField sx={{}} {...props}/>;
};

export default TextFieldUi;
